version: '3.4'
services:
  weaviate:
    image: semitechnologies/weaviate:latest
    restart: unless-stopped
    ports:
     - "8080:8080"
    environment:
      QUERY_DEFAULTS_LIMIT: 20
      AUTHENTICATION_ANONYMOUS_ACCESS_ENABLED: 'true'
      PERSISTENCE_DATA_PATH: "./data"
      DEFAULT_VECTORIZER_MODULE: text2vec-transformers
      ENABLE_MODULES: text2vec-transformers
      TRANSFORMERS_INFERENCE_API: http://t2v-transformers:8080
      CLUSTER_HOSTNAME: 'node1'
    volumes:
      - ./data/semantic_filters_data/weaviate_data:/data

  t2v-transformers:
    image: semitechnologies/transformers-inference:sentence-transformers-paraphrase-multilingual-MiniLM-L12-v2
    restart: unless-stopped
    environment:
      ENABLE_CUDA: 0
      # NVIDIA_VISIBLE_DEVICES: all # enable if running with CUDA

  yaserviceru:
    build:
      context: .
      dockerfile: Dockerfile
    #image: osuzdalev/yaserviceru:latest
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8081:80"
    depends_on:
      - weaviate
      - t2v-transformers
    volumes:
      - ./data/resources:/data/resources
      - ./data/semantic_filters_data/filters:/data/semantic_filters_data/filters

  postgres:
    image: "postgres:15.3"
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - ./data/resources/pg_data:/var/lib/postgresql/data # The data. This will increase in size with time. !!! DON'T COMMIT !!!
      - ./data/resources/pg_config:/etc/postgresql/ # The config files. !!! DON'T COMMIT !!!
    command: postgres -c config_file=/etc/postgresql/postgres.conf